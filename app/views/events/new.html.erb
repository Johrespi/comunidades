<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Crea un nuevo evento para la comunidad <%= @community.name %>!</h1>

  <%= render "form", community: @community, event: @event %>

</div>
